The file contains the clean dataset which i gave cleaned using power query
where trimed and clean corret data type 
where remove blank rows ( empty cells ) which where showing null by replacing value with N/A
corect the date by changing the data type from text to date and used locale to make show the date was set to english( united kingdom) 
Here is the image Project-1-images which is in project1 image a sub branch of the main 
